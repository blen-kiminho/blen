spring:
  profiles:
    active: prod

  main:
    allow-bean-definition-overriding: true
    banner-mode: "off"

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB